--extra-index-url https://download.pytorch.org/whl/cpu
streamlit
tensorflow-cpu
torch
torchvision
https://github.com/ultralytics/ultralytics/archive/refs/heads/main.zip
opencv-python-headless
numpy
pandas
matplotlib
seaborn
scikit-learn
pillow
tqdm
streamlit>=1.32
numpy
pandas
plotly

# IMPORTANT: use the headless build on Streamlit Community Cloud.
# The regular `opencv-python` package needs system OpenGL/GUI libraries
# (libGL.so.1 etc.) that the Cloud base image doesn't ship, and is a common
# cause of the app crashing right after a video/image is processed.
opencv-python-headless

tensorflow-cpu

# Installed straight from Ultralytics' GitHub source (not the PyPI name),
# per the FA-2 Step 7 setup. Pin to a tag/commit if you want reproducible
# builds instead of always pulling the tip of main.
git+https://github.com/ultralytics/ultralytics.git

streamlit>=1.32

# Pinned below 2.0 on purpose: tensorflow-cpu (and several of its
# transitive deps) have a long history of failing to resolve or crashing
# at import time against numpy>=2.0. An unpinned `numpy` here is a common
# cause of pip's resolver exiting non-zero during a fresh Cloud build.
numpy<2

pandas
plotly

# IMPORTANT: use the headless build on Streamlit Community Cloud.
# The regular `opencv-python` package needs system OpenGL/GUI libraries
# (libGL.so.1 etc.) that the Cloud base image doesn't ship, and is a common
# cause of the app crashing right after a video/image is processed.
opencv-python-headless

# Pinned to a version known to work with numpy<2 and with .h5 Keras
# models saved by older TF/Keras. If your fall_detection_model.h5 was
# trained against a different TF version, match this to that version
# instead — mismatches here can fail silently or throw on model load.
tensorflow-cpu==2.15.0

# Installed straight from Ultralytics' GitHub source (not the PyPI name),
# per the FA-2 Step 7 setup. Pinned to a release tag rather than the tip
# of main — main can (and does) bump its torch/numpy floor without
# warning, which is a common cause of a resolver conflict with the
# tensorflow-cpu pin above and a non-zero install exit.
git+https://github.com/ultralytics/ultralytics.git@v8.3.0
